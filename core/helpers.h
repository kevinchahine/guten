#pragma once

#include "../GutenExports.h"
#include <iostream>

namespace guten
{
	namespace core {
		GUTEN_API void eraseLine(std::ostream & os = std::cout);
	} // namespace core
} // namespace guten